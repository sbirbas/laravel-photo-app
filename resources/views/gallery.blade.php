@extends('layouts.app')
@section('content')
    <h1>Photos</h1>
{{--    something like this to loop over all the photos(which are managed in auth--}}
{{--    @foreach($photos as $photo) @endforeach--}}
@endsection
